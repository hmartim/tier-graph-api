{
  "$schema": "../../schemas/core/derived-relation.schema.json",
  "id": "rel-persist-0001",
  "sourceEntityId": "ent-state",
  "predicate": "guarantees",
  "targetEntityId": "ent-social-right",
  "predicateFamily": "attributional",
  "canonicalLabel": "The State guarantees the social right.",
  "qualifiers": {
    "polarity": {
      "dimension": "polarity",
      "status": "specified",
      "value": "affirmative"
    },
    "condition": {
      "dimension": "condition",
      "status": "absent",
      "note": "No precondition attaches to this guarantee in this example."
    },
    "jurisdiction": {
      "dimension": "jurisdiction",
      "status": "specified",
      "value": "example-jurisdiction"
    }
  },
  "reviewStatus": "accepted",
  "description": "Synthetic example: a persistent affirmative relation whose evidential state is expected to remain 'supported' across successive source states, because a supporting evidence basis is admissible throughout (see conformance case T01). This object carries NO temporal interval; persistence is a computed property of evidence admissibility, not a stored field. The `predicateFamily` and `predicate` tokens are not core constants: they are declared by the governing ProfileVocabulary (here, the generic vocabulary template)."
}
